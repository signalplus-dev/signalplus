var SignalIcon = React.createClass({
  render: function() {
    var iconPath = this.props.path + '.png'
    return (
      <img src={'<%= image_url("icons/offer.png") %>'} className='panel-icon' />
    );
  }
});
